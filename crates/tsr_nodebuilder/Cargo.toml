[package]
name = "tsr_nodebuilder"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = ["crates-io"]
description = "Type-to-syntax builder contracts and flags for ts-rust"
repository.workspace = true
readme = "README.md"
include = ["/src/**", "/Cargo.toml", "/README.md", "/LICENSE", "/NOTICE", "/licenses/**"]

[dependencies]
tsr_ast = { version = "0.1.0", path = "../tsr_ast" }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
