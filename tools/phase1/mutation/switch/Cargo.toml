[package]
name = "phase1_mutants"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Phase 1 mutation witnesses: the runtime mutant switch spliced into scratch copies of the workspace; inert in the repository"

[dependencies]

[lints]
workspace = true
