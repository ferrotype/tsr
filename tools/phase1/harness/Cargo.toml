[package]
name = "phase1_harness"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Private Phase 1 probe outcome and dispatch contract"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }

[lints]
workspace = true
